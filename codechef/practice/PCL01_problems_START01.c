// Write a program that takes a number as the input, and prints it to the output.

#include <stdio.h>

int main(void) {
	// your code goes here
    int n;
    scanf("%d", &n);
    printf("%d", n);

    return 0;
}